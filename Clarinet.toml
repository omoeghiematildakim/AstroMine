[project]
name = 'AstroMine'
description = ''
authors = []
telemetry = true
cache_dir = './.cache'
requirements = []
[contracts.asteroid-mining-token]
path = 'contracts/asteroid-mining-token.clar'
clarity_version = 2
epoch = 2.5

[contracts.mining-simulation]
path = 'contracts/mining-simulation.clar'
clarity_version = 2
epoch = 2.5
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
